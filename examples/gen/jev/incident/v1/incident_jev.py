"""Generated by protoc-gen-jev. DO NOT EDIT."""
from dataclasses import dataclass, asdict
from typing import Any, Dict, List, Optional
from typesafe_sdk import TypeSafeClient, Choice, Noul, Score

class TriageResponseJevClient:
    """Typed Jev client for evaluating TriageResponse."""

    def __init__(self, client: Optional[TypeSafeClient] = None, api_key: Optional[str] = None):
        if client is not None:
            self._client = client
        elif api_key is not None:
            self._client = TypeSafeClient(api_key=api_key)
        else:
            self._client = None  # Will lazy-init on evaluate() from env

    @property
    def client(self) -> TypeSafeClient:
        if self._client is None:
            self._client = TypeSafeClient()
        return self._client

    def build_questions(self) -> Dict[str, Any]:
        """Constructs the native Jev question primitives from the Protobuf schema."""
        return {
            "routing_target": Choice(
                instructions="1. Oneof mutual exclusion -> Jev Choice question Jev selects the single most appropriate routing target based on input context.",
                criteria={"automated_runbook": None, "incident_commander": None, "oncall_engineer": None},
            ),
            "requiresImmediatePaging": Noul(
                instructions="Does this incident indicate active user-facing outage requiring paging?",
            ),
            "priority": Choice(
                instructions="Assess the operational severity and customer blast radius",
                criteria={"PRIORITY_LEVEL_CRITICAL": None, "PRIORITY_LEVEL_HIGH": None, "PRIORITY_LEVEL_LOW": None, "PRIORITY_LEVEL_MEDIUM": None},
            ),
            "urgencyRating": Score(
                instructions="How urgently does this issue need to be resolved?",
                criteria=["1", "2", "3", "4", "5"],
            ),
            "blastRadiusPercentage": Score(
                instructions="Estimated percentage of production infrastructure impacted",
                criteria=["0.0", "25.0", "50.0", "75.0", "100.0"],
            ),
            "complianceClassification": Choice(
                instructions="Categorize any sensitive or regulated data exposed in the incident report",
                criteria={"INTERNAL_CONFIDENTIAL": None, "PCI_DSS": None, "PUBLIC": None, "RESTRICTED_PII": None},
            ),
        }

    def evaluate(self, state: Dict[str, Any] | str) -> Dict[str, Any]:
        """Evaluates state against Jev System One questions and returns typed decisions."""
        payload_state = {"text": state} if isinstance(state, str) else state
        questions = self.build_questions()
        resp = self.client.system_one(state=payload_state, questions=questions)
        out = {}
        if "routing_target" in resp.choices:
            out["routing_target"] = resp.choices["routing_target"].choice
        if "requiresImmediatePaging" in resp.nouls:
            _item = resp.nouls["requiresImmediatePaging"]
            out["requires_immediate_paging"] = getattr(_item, "result", getattr(_item, "noul", False))
        if "priority" in resp.choices:
            out["priority"] = resp.choices["priority"].choice
        if "urgencyRating" in resp.scores:
            out["urgency_rating"] = resp.scores["urgencyRating"].score
        if "blastRadiusPercentage" in resp.scores:
            out["blast_radius_percentage"] = resp.scores["blastRadiusPercentage"].score
        if "complianceClassification" in resp.choices:
            out["compliance_classification"] = resp.choices["complianceClassification"].choice
        return out

    def batch_evaluate(self, states: List[Dict[str, Any] | str]) -> List[Dict[str, Any]]:
        """Evaluates a batch of states against Jev."""
        return [self.evaluate(s) for s in states]

@dataclass
class TriageRequest:
    """Input request message for Triage."""
    incident_id: str = ""
    title: str = ""
    description: str = ""
    raw_logs: str = ""

@dataclass
class TriageResponse:
    """Output decisions message for Triage."""
    routing_target: Optional[str] = None
    requires_immediate_paging: Optional[bool] = None
    priority: Optional[str] = None
    urgency_rating: Optional[float] = None
    blast_radius_percentage: Optional[float] = None
    compliance_classification: Optional[str] = None

class IncidentTriageServiceClient:
    """Typed Jev client for IncidentTriageService."""

    def __init__(self, client: Optional[TypeSafeClient] = None, api_key: Optional[str] = None):
        if client is not None:
            self._client = client
        elif api_key is not None:
            self._client = TypeSafeClient(api_key=api_key)
        else:
            self._client = None

    @property
    def client(self) -> TypeSafeClient:
        if self._client is None:
            self._client = TypeSafeClient()
        return self._client

    def build_triage_questions(self) -> Dict[str, Any]:
        """Constructs the native Jev question primitives from the Protobuf schema."""
        return {
            "routing_target": Choice(
                instructions="1. Oneof mutual exclusion -> Jev Choice question Jev selects the single most appropriate routing target based on input context.",
                criteria={"automated_runbook": None, "incident_commander": None, "oncall_engineer": None},
            ),
            "requiresImmediatePaging": Noul(
                instructions="Does this incident indicate active user-facing outage requiring paging?",
            ),
            "priority": Choice(
                instructions="Assess the operational severity and customer blast radius",
                criteria={"PRIORITY_LEVEL_CRITICAL": None, "PRIORITY_LEVEL_HIGH": None, "PRIORITY_LEVEL_LOW": None, "PRIORITY_LEVEL_MEDIUM": None},
            ),
            "urgencyRating": Score(
                instructions="How urgently does this issue need to be resolved?",
                criteria=["1", "2", "3", "4", "5"],
            ),
            "blastRadiusPercentage": Score(
                instructions="Estimated percentage of production infrastructure impacted",
                criteria=["0.0", "25.0", "50.0", "75.0", "100.0"],
            ),
            "complianceClassification": Choice(
                instructions="Categorize any sensitive or regulated data exposed in the incident report",
                criteria={"INTERNAL_CONFIDENTIAL": None, "PCI_DSS": None, "PUBLIC": None, "RESTRICTED_PII": None},
            ),
        }

    def triage(self, req: TriageRequest | Dict[str, Any]) -> TriageResponse:
        """Evaluates TriageRequest against Jev and returns a typed TriageResponse."""
        payload_state = asdict(req) if isinstance(req, TriageRequest) else req
        questions = self.build_triage_questions()
        resp = self.client.system_one(state=payload_state, questions=questions)
        out = TriageResponse()
        if hasattr(resp, "answers") and "routing_target" in resp.answers:
            out.routing_target = getattr(resp.answers["routing_target"], "choice", None)
        elif "routing_target" in resp.choices:
            out.routing_target = resp.choices["routing_target"].choice
        if hasattr(resp, "answers") and "requiresImmediatePaging" in resp.answers:
            _item = getattr(resp.answers["requiresImmediatePaging"], "noul", None)
            out.requires_immediate_paging = _item if isinstance(_item, bool) else (_item >= 0.5 if isinstance(_item, (int, float)) else False)
        elif "requiresImmediatePaging" in resp.nouls:
            _item = resp.nouls["requiresImmediatePaging"]
            out.requires_immediate_paging = getattr(_item, "result", getattr(_item, "noul", False))
        if hasattr(resp, "answers") and "priority" in resp.answers:
            out.priority = getattr(resp.answers["priority"], "choice", None)
        elif "priority" in resp.choices:
            out.priority = resp.choices["priority"].choice
        if hasattr(resp, "answers") and "urgencyRating" in resp.answers:
            out.urgency_rating = getattr(resp.answers["urgencyRating"], "score", None)
        elif "urgencyRating" in resp.scores:
            out.urgency_rating = resp.scores["urgencyRating"].score
        if hasattr(resp, "answers") and "blastRadiusPercentage" in resp.answers:
            out.blast_radius_percentage = getattr(resp.answers["blastRadiusPercentage"], "score", None)
        elif "blastRadiusPercentage" in resp.scores:
            out.blast_radius_percentage = resp.scores["blastRadiusPercentage"].score
        if hasattr(resp, "answers") and "complianceClassification" in resp.answers:
            out.compliance_classification = getattr(resp.answers["complianceClassification"], "choice", None)
        elif "complianceClassification" in resp.choices:
            out.compliance_classification = resp.choices["complianceClassification"].choice
        return out

    def batch_triage(self, reqs: List[TriageRequest | Dict[str, Any]]) -> List[TriageResponse]:
        """Evaluates a batch of TriageRequest items against Jev."""
        return [self.triage(r) for r in reqs]

