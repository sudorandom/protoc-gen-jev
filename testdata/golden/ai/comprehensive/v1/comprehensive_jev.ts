// Code generated by protoc-gen-jev. DO NOT EDIT.
import { TypeSafeClient, choice, score, noul } from "@typesafe-ai/sdk";

export interface MetadataJevDecisions {
  version?: number;
}

export class MetadataJevClient {
  private client: TypeSafeClient;

  constructor(client?: TypeSafeClient) {
    this.client = client ?? new TypeSafeClient();
  }

  buildQuestions() {
    return {
      version: score(
        "Evaluate version",
        ["1", "2", "3", "4", "5"],
      ),
    };
  }

  async evaluate(state: Record<string, any> | string): Promise<MetadataJevDecisions> {
    const payloadState = typeof state === "string" ? { text: state } : state;
    const resp = await this.client.systemOne({ state: payloadState, questions: this.buildQuestions() });
    const anyResp = resp as any;
    return {
      version: anyResp.answers?.["version"]?.score ?? anyResp.scores?.["version"]?.score,
    };
  }

  async batchEvaluate(states: Array<Record<string, any> | string>): Promise<MetadataJevDecisions[]> {
    return Promise.all(states.map(state => this.evaluate(state)));
  }
}

export interface ComprehensiveRecordJevDecisions {
  scoreInt?: number;
  bigCount?: number;
  ratio?: number;
  latitude?: number;
  isEnabled?: boolean;
  status?: string;
  payload?: string;
  churnRisk?: boolean;
  accountTier?: string;
}

export class ComprehensiveRecordJevClient {
  private client: TypeSafeClient;

  constructor(client?: TypeSafeClient) {
    this.client = client ?? new TypeSafeClient();
  }

  buildQuestions() {
    return {
      scoreInt: score(
        "--- Numeric validation variants ---",
        ["1", "26", "51", "75", "100"],
      ),
      bigCount: score(
        "Evaluate bigCount",
        ["1", "2", "3", "4", "5"],
      ),
      ratio: score(
        "Evaluate ratio",
        ["0.0", "0.25", "0.5", "0.75", "1.0"],
      ),
      latitude: score(
        "Evaluate latitude",
        ["-90.0", "-45.0", "0.0", "45.0", "90.0"],
      ),
      isEnabled: noul(
        "--- Boolean & Enum ---",
      ),
      status: choice(
        "Evaluate status",
        { "STATUS_ACTIVE": null, "STATUS_ARCHIVED": null, "STATUS_PAUSED": null },
      ),
      payload: choice(
        "--- Oneof ---",
        { "binary_payload": null, "text_payload": null },
      ),
      churnRisk: noul(
        "Custom Jev instruction: assess customer cancellation intent",
      ),
      accountTier: choice(
        "Discrete string choices via Jev options",
        { "TIER_ENTERPRISE": null, "TIER_PREMIUM": null, "TIER_STANDARD": null },
      ),
    };
  }

  async evaluate(state: Record<string, any> | string): Promise<ComprehensiveRecordJevDecisions> {
    const payloadState = typeof state === "string" ? { text: state } : state;
    const resp = await this.client.systemOne({ state: payloadState, questions: this.buildQuestions() });
    const anyResp = resp as any;
    return {
      scoreInt: anyResp.answers?.["scoreInt"]?.score ?? anyResp.scores?.["scoreInt"]?.score,
      bigCount: anyResp.answers?.["bigCount"]?.score ?? anyResp.scores?.["bigCount"]?.score,
      ratio: anyResp.answers?.["ratio"]?.score ?? anyResp.scores?.["ratio"]?.score,
      latitude: anyResp.answers?.["latitude"]?.score ?? anyResp.scores?.["latitude"]?.score,
      isEnabled: anyResp.answers?.["isEnabled"]?.noul ?? anyResp.nouls?.["isEnabled"]?.result,
      status: anyResp.answers?.["status"]?.choice ?? anyResp.choices?.["status"]?.choice,
      payload: anyResp.answers?.["payload"]?.choice ?? anyResp.choices?.["payload"]?.choice,
      churnRisk: anyResp.answers?.["churnRisk"]?.noul ?? anyResp.nouls?.["churnRisk"]?.result,
      accountTier: anyResp.answers?.["accountTier"]?.choice ?? anyResp.choices?.["accountTier"]?.choice,
    };
  }

  async batchEvaluate(states: Array<Record<string, any> | string>): Promise<ComprehensiveRecordJevDecisions[]> {
    return Promise.all(states.map(state => this.evaluate(state)));
  }
}

